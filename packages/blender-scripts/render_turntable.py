from .scene_setup import compute_direction_angles

def render_turntable(directions: int, frames: int):
    # Placeholder render loop
    return { 'directions': directions, 'frames': frames, 'angles': compute_direction_angles(directions) }
